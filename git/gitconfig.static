[core]
        excludesfile = ~/.gitignore.global
[color]
        ui = auto
[push]
        default = simple
[pull]
        # Only allow fast-forward merges
        ff = only
[alias]
        # Console tree view
        tree = log --graph --oneline --decorate
