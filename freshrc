# handles updating fresh
fresh freshshell/fresh bin/fresh --bin

if test -n "$BASH_VERSION" ; then
  fresh freshshell/fresh contrib/source-build.sh --file='~/.bashrc'
  fresh erichmachado/dotfiles bash/bash_profile --file
fi
